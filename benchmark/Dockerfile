FROM python:3.8-slim-buster

# Install necessary packages
RUN apt-get update && apt-get install -y \
    less \
    git \
    build-essential \
    libpq-dev \
    libffi-dev \
    libssl-dev \
    libxml2-dev \
    libxslt1-dev \

